# Notebooks

Estes notebooks descrevem o todo o processo de correr os modelos, desde pré-processamento até à construção e treino dos modelos

Na construção de cada modelo são indicados os parâmetros que foram utilizados no pré-processamento.
